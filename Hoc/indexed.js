const array = [];
array.push(1);
console.log(array);
// arrow function
function sum(a, b) {
    return a + b;
};
// expression function support with hoisting
const multil= function(a, b) {
    return a*b;
}
// anonymous function not support with hoisting
const sub = (a, b)=>{
    return a - b;
}
// arrow function not support with hoisting
// template literal
const myName = 'Mindx';

import math from myMath.js;
console.log(math.sum(5,6)); 
// concate array dùng để nối 2 array
// ...array, ...object dùng để trải phẳng
Math.max = số to nhất

git add remote.origin dùng để liên kết local